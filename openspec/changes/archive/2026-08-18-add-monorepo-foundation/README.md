# add-monorepo-foundation

Bootstrap Turborepo + pnpm workspace tooling, shared tsconfig/lint bases, shared-infra docker-compose, CI skeleton, env template, and agiliz-cli registration groundwork so the first real backend microservice has a working foundation to land on
