# add-worker-integration-tests

Close the automated integration-test debt recorded by gateway, sales and supply: real queue paths against Redis, and the gateway's failure semantics through its real HTTP stack
