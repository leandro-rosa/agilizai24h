## Implementation status (2026-09-19)

**On hold by the operator:** the analytic implementation (groups 6 to 11) does not start until the real monthly reports are imported through `add-drive-ingestion-source`, the quality of `Cupom` and of the history is validated, and the calibration of group 5 is reviewed and approved. **May proceed now** (architecture and UI without freezing any analytic rule): groups 1 to 4. **Done (2026-09-19):** groups 1 to 4 — the shell, the parameters, the measurement layer and the coverage diagnostic are built and verified; groups 5 to 11 have not been started. **Revised (2026-09-19, later):** the operator separated business rules, data-quality rules and the analytic model, asked that no rule that shapes recommendations live only in a browser, and set 25 principles for the intelligence (design D16 and D19 to D23, spec requirements added). Tasks 3.8 to 3.14 carried the delta into the code already built and are **done and verified (2026-09-19)**; the analytic groups below now also carry the principles; the backend for the official register, versions and decisions is the separate change `add-commercial-intelligence-governance`. The business-rule part of this change is **not** considered done until that change exists. Every number named in these tasks is a provisional default read from the configuration of group 3, never a constant in the code.

## 1. Preparation

- [x] 1.1 Read the relevant guides in `frontend/apps/admin/node_modules/next/dist/docs/` for App Router client pages, search params and streaming (`frontend/apps/admin/AGENTS.md` warns this Next.js version has breaking changes); note anything that differs from the existing pages. _Read (2026-09-19, Next 16.3.0): a client page under the `(app)` group is the same shape as the existing ones, and the filters live in component state, so search params and streaming are not used. The one thing that matters here is `NEXT_PUBLIC_*`: it is inlined at build time only when written literally as `process.env.NEXT_PUBLIC_X`; `process.env[name]` and `const env = process.env` are not inlined. So the parameters' environment reader lists every variable literally (`env.ts`), and the docs state that a value change needs a rebuild (`admin-prod` takes it as a build arg)._
- [x] 1.2 Record the baseline of `pnpm turbo run lint typecheck build` on the current (dirty) tree, so any new failure is attributable to this change. _Baseline for `@agiliz/admin`, measured 2026-09-19 before any change to this feature: `tsc --noEmit` clean; `pnpm lint` 0 errors and 2 warnings, both in `src/app/(app)/ingestion/page.tsx` (`react-hooks/incompatible-library` on `form.watch`, unused `value`); production `next build` clean with 26 routes (run in an isolated container, because the host's `.next` belongs to root)._
- [x] 1.3 Confirm the working tree's uncommitted base (`sales-insights.ts`, `loss-insights.ts`, `store-insights.ts`, `components/sales/`, `components/supply/`, `lib/api/sales.ts`, transaction backend) is present and builds; do not stage or commit any of it as part of this change. _Present and built in the baseline above; nothing is staged or committed._

## 2. Navigation and page shell (may proceed now)

- [x] 2.1 Add the `Sparkles` import and the "Inteligência Comercial" item (`/commercial-intelligence`, `sales:read`) after "Vendas" in the "Operação" group of `src/components/app-sidebar.tsx`
- [x] 2.2 Create `src/app/(app)/commercial-intelligence/page.tsx`: `PageHeader`, `FiltersBar` (Store/Network default Network with no "select a store" gate, Period = last 24 months plus the current month labelled "em andamento", Comparison, Category) and the five-tab skeleton _The Category control is present but switched off, with the reason printed under the filters: the analyses that use it are held (design D13 updated)._
- [x] 2.3 Wire the first-wave hooks (stores, products, network transactions for the period) and the page-level `RequestState`, including the "no transaction detail for this period" empty state and the retry on error _The second wave was added with 4.4._ _The empty state became its own component (`NoTransactionDetail`) after the operator opened the page on a local stack whose `sales_transaction` table has no rows in any month: it now says that the detail exists only for months imported from the network-wide sales report, that the per-SKU months `/sales` shows carry none of it, and how to import one (Ingestão › Vendas with the store left blank, or the Drive reading once configured), with a link when the viewer may read Ingestão. The first wording guessed "a month before the format", which was wrong there._
- [x] 2.4 Verify: the item is hidden without `sales:read`; only this item is highlighted and the breadcrumb reads "Inteligência Comercial"; switching the store issues no new `/sales/*/transactions` request (browser network panel) _Verified in a browser against a local mock gateway: the item is absent, together with "Vendas", for a user without `sales:read`; only "Inteligência Comercial" is highlighted and the breadcrumb reads the same; switching the store issued no request at all (the mock's request log stayed empty)._

## 3. Parameters and foundations (may proceed now; values stay provisional)

- [x] 3.1 `lib/commercial-intelligence/types.ts`: `Scope`, `Provenance`, `Level`, `Range`, `ConfidenceFactor`, `Confidence`, `EvidenceFact`, `Opportunity` and the dataset/analysis shapes, with no enums or namespaces
- [x] 3.2 `lib/commercial-intelligence/parameters.ts` per design D16: one typed `parameters` object with documented defaults for every threshold and assumption of D2–D10 and D12, overridable by `NEXT_PUBLIC_CI_*` environment variables; engine functions take the parameters as an argument and no module keeps its own constants; the minimum margin (default 30%) and the capture range (default 20–60%) are also adjustable per viewer and remembered in `localStorage` inside `try/catch`, the page working without it _88 parameters (`PARAMETER_DOCS` is typed over every numeric path, so an undocumented one fails the typecheck); `env.ts` spells out each `NEXT_PUBLIC_CI_*` literally because Next inlines nothing else; the per-viewer overrides live in `use-parameters.ts` (`useSyncExternalStore`, with an in-memory copy when storage throws)._ _**Superseded in part (2026-09-19, later):** the per-viewer adjustment and `use-parameters.ts` were removed by 3.9, and the table now has 95 parameters, each tagged by kind (3.8). The environment override, the typed table and the rule that no module keeps its own constant stand._
- [x] 3.3 `ParametersPanel`: each parameter with its value in use, its default and a "provisório" mark while the calibration of group 5 is not approved; the detail of every recommendation will list the parameter values that shaped it _The per-recommendation list of the parameter values that shaped it arrives with group 11 (`Opportunity.parameters` is already in the type)._ _**Superseded by 3.10:** `ParametersPanel` was removed; the catalog of parameters lives in the advanced page `/commercial-intelligence/calibration` (`parameter-catalog.tsx`), out of the main experience._
- [x] 3.4 `lib/commercial-intelligence/confidence.ts`: the graduated rubric and hard caps of D9 reading their numbers from the parameters, and the "Dados insuficientes para recomendar…" message builder
- [x] 3.5 `ProvenanceBadge`, `ConfidenceBadge` and `SuggestionTag` in `components/commercial-intelligence/`, built on `StatusBadge`, with no design-token change _`SuggestionTag` is ready and not yet used: the recommendations are held._
- [x] 3.6 Synthetic-marker guard: a pure `isSynthetic(name)` and the exclusion of any store or product carrying the marker unless `NEXT_PUBLIC_ALLOW_SYNTHETIC` is set, reported as a banner item
- [x] 3.7 Verify: confidence truth table (clean pair → High; coverage inside the partial band → capped Medium; flagged SKU with loss as main driver → capped Low; contradictory stability → capped Low; evidence below the gate → insufficient); changing the minimum margin or the capture range takes effect without a rebuild and does not persist beyond the viewer; a marked store is excluded on the real gateway and included, labelled, only with the mock flag; typecheck; badges legible in light and dark _Confidence truth table (15 checks) plus the parameter and synthetic-guard checks pass in a throwaway script outside the repo, under UTC, America/Sao_Paulo and Asia/Tokyo. In a browser against the mock: the minimum margin and the capture range change on the spot with no rebuild and no request, persist across a reload in this browser only, refuse a floor above the ceiling and work with `localStorage` blocked; every store and product marked synthetic is excluded on a page without the mock flag (with the banner saying so) and kept, labelled, with it; badges are legible in light and dark. The simulator's recomputation at 40% belongs to group 7._ _**Superseded in part by 3.9 and 3.14:** the in-browser adjustment of the minimum margin and of the capture range checked here no longer exists (no rule is stored in a browser); the confidence truth table, the synthetic guard, the environment checks and the badges stand._
- [x] 3.8 Tag every parameter with a `kind` (`business`, `quality` or `analytic`) in the typed table and document each one: name, purpose, formula, unit, current value, reason for the default, where it is used, and the effect of raising and of lowering it. Retire `impact.captureLow` and `impact.captureHigh` for `impact.scenarioConservative`, `impact.scenarioExpected` and `impact.scenarioOptimistic` (0.2 / 0.4 / 0.6); add `impact.mainScreenItems` (business, 5) and the availability limits of D19 (history months, dated-lines share, eligible-SKU revenue share). Business rules are the minimum combo margin, the largest discount worth testing, the impact floor, the priority floor and the number of opportunities on the main screen _95 parameters, each with a kind and the full documentation (purpose, formula, unit, current value, reason for the default, where it is used, effect of raising and of lowering, environment variable); `PARAMETER_DOCS` is typed over every numeric path, so an undocumented parameter fails the typecheck. Exactly five business rules: minimum combo margin, largest discount worth testing, impact floor to be listed, impact floor for priority, number of opportunities on the main screen. `impact.scenarioConservative/Expected/Optimistic` (0.2 / 0.4 / 0.6) replaced the capture range._
- [x] 3.9 Remove the per-viewer overrides and their `localStorage` (`use-parameters.ts`, `parseOverrides`, `applyOverrides`, `VIEWER_ADJUSTABLE`): no rule that shapes recommendations may be stored in a browser (D16). Business rules are read-only, with their source stated (deployment default, official register pending) _`use-parameters.ts`, `parameters-panel.tsx`, `parseOverrides`, `applyOverrides` and `VIEWER_ADJUSTABLE` are gone; `runtime-parameters.ts` reads only the build-time environment. In a browser the storage holds the theme and nothing else, and the page has no numeric input._
- [x] 3.10 "Regras de negócio" sheet (read-only, with the pending-register notice) and the advanced page `/commercial-intelligence/calibration`, out of the main experience: parameters grouped by kind with their full documentation, every one marked "provisório", and a reserved place for the calibration report of 5.7 _`business-rules-sheet.tsx` (read-only, says the official register does not exist yet and that the value comes from the deployment default), `parameter-catalog.tsx` and the route `calibration/page.tsx`, reachable from the header and from the sheet; the calibration report is a reserved block marked "Em espera"._
- [x] 3.11 `availability.ts` (D19) and the "Qualidade dos dados" tab: for each analysis one of "Análise disponível", "Disponível com ressalvas" or "Dados insuficientes", with the indicators that decide it (coupon coverage, resolved costs, reconciliation, history, timestamps, sample sizes), the blocks and the caveats; the thresholds are shown as reference text, never as editable settings _Six analyses (combos, product return, loss, behavior, stores, history), each with its state, its indicators and the caveats or the blocks; the limits that separate the states are stated as provisional reference text pointing to the advanced page._
- [x] 3.12 "Estimativa de impacto" on the Overview: the three scenarios and the premise stated as a potential, not a measured or guaranteed result, with Observed next to Estimated _`ImpactEstimateCard` on the Overview, with the words "É uma premissa de potencial, não um resultado medido nem garantido"; the observed margin and the estimated range are labelled Observado and Estimado. The range itself waits for group 11._
- [x] 3.13 `LOGIC_VERSION`, exported by the engine, and `Opportunity.logicVersion` plus the parameter snapshot on the recommendation type (D22) _`logic-version.ts` (`ci-logic/0.1.0-provisional`), shown on the advanced page; `Opportunity.logicVersion` and `Opportunity.parameters` are in the type, filled by group 11._
- [x] 3.14 Verify: every parameter has a kind and its documentation (typecheck fails otherwise); the availability table for hand-built fixtures (open / partial / blocked gate, costs at the three bands, reconciliation missing / incomplete / complete, one month of history); no viewer-local override of a rule remains and two viewers see the same business rule; no threshold is presented as a setting outside the advanced area; typecheck; a browser check against the local mock in light and dark _Throwaway scripts outside the repo: 19 checks on the rules (95 parameters with kind and documentation, the five business rules, the three kinds partitioning every parameter, `env.ts` matching the parameters, scenario order, no override machinery exported, the logic version, the availability table for every analysis and every state) and 13 core checks under UTC, America/Sao_Paulo and Asia/Tokyo; `tsc` and ESLint exit 0. In a browser against the mock, in light and dark: the six states, the business-rules sheet, the advanced page with a parameter expanded, the storage holding only the theme and no editable threshold outside the advanced page. "Two viewers see the same business rule" holds by construction (one build-time source, nothing stored per viewer) and was not exercised with two live sessions._

## 4. Measurement plumbing (may proceed now; measures, decides nothing)

- [x] 4.1 `dataset.ts`: OK lines, per-line `priceState`, wall-clock day/hour/weekday through UTC accessors, coupon baskets with the span rule and orphan/dropped counts, per-store and per-SKU aggregates and day sets
- [x] 4.2 `loss-index.ts`: join the network reconciliation's per-store slice of the period by store and SKU (loss by reason, `inconsistent_stock`, `unvalued`, `complete`), per (store, SKU) loss quality, and the supply index for sell-through
- [x] 4.3 `quality.ts`: the coupon-coverage indicators of D17 (store coverage, pooled network coverage, revenue coverage, multi-item share, orphan lines) as measurements, the gate state derived from the parameters, unresolved-cost SKUs and revenue share, stores without detail, reconciliation quality, catalog-vocabulary counts, partial period _The vocabulary item counts what the catalog says and states that meal-based analyses are limited by it, without guessing from product names._
- [x] 4.4 Wire the second-wave hooks (comparison transactions, dated costs keyed on the network's sorted SKUs, six-month reconciliation, supply) after the transactions resolve _It also fetches the comparison period's own costs (design D13 updated): a fair previous margin needs the cost as it stood then. Sources the viewer may not read are not requested and read "Sem permissão"._
- [x] 4.5 Overview KPIs (Receita, Ticket médio, Itens por compra, Margem por compra, Margem total, Margem após perdas) with provenance and delta or "Sem comparação"; "Margem após perdas" in its own `RequestState`, "sobre N de M lojas" when partial; "Oportunidades identificadas" shows "—" until group 11
- [x] 4.6 `DataQualityBanner`: items ordered critical > attention > info, each stating its effect; expanded when any item is critical or attention _The number of recommendations a reduced-confidence store affects comes with group 11; until then the item states the effect without a count._
- [x] 4.7 Coverage diagnostic view (read-only, in the Combos tab): per-store coverage with the parameter thresholds drawn as reference lines, the multi-item share, and the coupon-versus-coupon-less comparison by category mix, average line value, hour of day and POS or machine model; it feeds group 5 and recommends nothing
- [x] 4.8 Verify: with every SKU cost resolved, Receita, Ticket médio, Itens por compra and margin equal `/sales` for the same scope; an unresolved-cost SKU is excluded from margin and counted; `2026-08-31T23:58:47Z` → hour 23, Monday and `2026-08-31T01:30:00Z` → hour 1, Monday; a coverage fixture (0.98 / 0.70 / 0) leaves only the first store eligible under the default thresholds _Run in the throwaway script (50 checks, three time zones) and against the mock: with every cost resolved Receita, Ticket médio, Itens por compra and margin equal `/sales`' own helper; with one SKU without cost mine is R$ 7.571,15 and `/sales`' is R$ 8.113,65, the documented overstatement; `2026-08-31T23:58:47Z` reads hour 23 and Monday, `2026-08-31T01:30:00Z` hour 1 and Monday, in every time zone; the coverage fixture 98% / 70% / 0% leaves only the first store eligible. The figures on screen were also recomputed from the mock's data and matched to the cent._

## 5. Calibration gate (needs the real months; blocks groups 6 to 11)

- [ ] 5.1 Wait for the operator to confirm which real months were imported through `add-drive-ingestion-source` and that `/sales` shows them; record the months and the stores present
- [ ] 5.2 Coverage distribution: `storeCoverage` for every store-month, as a histogram against the bands of D17 (`≥ 95%`, `[80%, 95%)`, `< 80%`), plus the multi-item share and the orphan-line rate
- [ ] 5.3 Bias diagnostic: coupon lines versus coupon-less lines on category mix, average line value, hour of day, POS and machine model, and store; state whether the missingness looks informative
- [ ] 5.4 Sensitivity run: recompute the top pairs and the cart gaps with stores `≥ 95%` only versus all stores `≥ 80%` (a throwaway script outside the repo over the real months) and report how much the top-ten lists overlap
- [ ] 5.5 History check: months available, whether the comparison month exists, stability across months, and the category vocabulary of the real data (including whether a point-of-sale category is available for a later decision)
- [ ] 5.6 Present the results and obtain the operator's approval of the defaults for the coupon thresholds, the basket minimums, the capture range and the minimum-margin default; write the approved values back into design D17 and `parameters.ts` (and, once it exists, into the official register of `add-commercial-intelligence-governance`), and clear the "provisório" mark only for the approved parameters
- [ ] 5.7 The calibration report of D23: the distributions of coupon coverage, sample sizes and cost resolution over the real months, and how the provisional parameters would behave on them: stores that would be excluded, products left without data, pairs that would pass the filters, opportunities that would be generated, recommendations that would be blocked. Nothing is tuned to make results appear; the aim is to avoid false signals without blocking useful analyses

## 6. Combos and cart gaps (on hold until 5.6)

- [ ] 6.1 `associations.ts`: pair counts, support/confidence/lift, lift lower bound, reliability rule, ranking by excess, network top pairs and per-store deviations (Wilson bound), all thresholds from the parameters
- [ ] 6.2 Classification of D4 (strong association / cross-sell potential / promotional opportunity), `fullPriceShare` and the cannibalization warning
- [ ] 6.3 `cart-gaps.ts`: ordered category pairs, gap rate, with-versus-without ticket and margin over cost-resolved baskets, reference rules and gates of D10 and D17
- [ ] 6.4 Combos tab: coverage card with state pill and excluded stores, the missing-from-cart section (top 3 gaps), and the pair table (class filter chips, up to 50 rows plus "ver mais", a "Simular" action per row), as a `memo` component with stable props
- [ ] 6.5 Verify with hand-computed fixtures at the default parameters: N=10, nA=nB=4, nAB=3 → lift 1.875, lower bound ≈ 0.98, not reliable; N=1000, nA=nB=100, nAB=40 → lift 4.0, lower bound ≈ 3.02; nAB=25 → strong association, nAB=22 → cross-sell (or promotional with a demand reason and a feasible discount), nAB=15 → not reliable; three lines with two sharing coupon "C1" → 1 basket, 1 orphan, coverage 2/3; each gate state renders
- [ ] 6.6 Suggestion order per D20: joint exposure, communication, positioning and cross-sell before any price action; a discount only with a demand-side reason, a feasible discount within the largest discount the business rule allows, and the minimum margin preserved

## 7. Combo simulator (on hold until 5.6)

- [ ] 7.1 `combo-simulator.ts`: the formulas of D12, the observed-price rule (mode over the last 7 days with at least 5 observations, else the whole period) and the refusal on unresolved cost, the minimum margin taken from the parameters
- [ ] 7.2 `ComboSimulator` component (`key` on the selected pair): editable proposed price and minimum margin labelled assumptions, single source of truth for R$ and % discount, status flags, maximum discount for the minimum margin, cannibalization line, list price as a cross-check via the price-as-of hook
- [ ] 7.3 Verify: P=1500, C=700, P'=1350 → margin 800 → 650 (53.3% → 48.1%), break-even extra volume 23.08%; P'=700 → "inviável"; minimum margin 30% → maximum discount 500 (33.3%) and at 40% the maximum discount is recomputed without a code change; an unresolved-cost product disables the simulation

## 8. Products (on hold until 5.6)

- [ ] 8.1 `product-returns.ts`: per-SKU metrics over cost-resolved lines, scope percentiles, eligibility ("Dados insuficientes", "Em observação", uncertain availability) and the class rules of D6, thresholds from the parameters
- [ ] 8.2 Loss-cause wording in `copy.ts` per D11, with no "remover/retirar" instruction and `other_reason` never described as theft
- [ ] 8.3 Products tab: class chips with counts and the table (Produto | Classe | Receita | Qtd | Margem R$ | Margem % | Perda R$ + causa | Margem após perdas | Giro | Disponibilidade (Estimado) | Confiança), plus the note that classes are relative to the scope
- [ ] 8.4 Verify: a 9-of-10-units product with 10 selling days is "Em observação"; a top-revenue product with negative margin after losses is "Baixo retorno econômico"; a 60% `other_reason` SKU's text contains neither "furto", "roubo" nor "remover"; an unresolved-cost SKU never shows 100% margin
- [ ] 8.5 No class from a single threshold (D20): combine volume, contribution, margin, availability, history, loss and store context; a new product, one restocked in fewer units than the minimum, or one with a possible stock-out reads "Em observação" with the signals that prevented a class, never an adverse class

## 9. Behavior (on hold until 5.6)

- [ ] 9.1 `hourly.ts`: dayparts, weekday/weekend, and rules H1–H3 of D7 with their minimums from the parameters
- [ ] 9.2 Behavior tab: weekday × hour heatmap (Receita | Compras | Ticket | Margem, cells below the minimum show "—" for ticket and margin), daypart × category stacked bars through `ChartContainer` with `var(--chart-N)`, attach-by-daypart table, insight list and the local-clock note
- [ ] 9.3 Verify: bucketing follows the UTC fixtures of 4.8; the new heatmap differs from the `/sales` heatmap by the known time-zone shift when viewed from a non-UTC browser

## 10. Stores (on hold until 5.6)

- [ ] 10.1 `peers.ts`: per-store features, 10-bin demand distribution, `1 − Hellinger` similarity, peer selection (minimum baskets, count ratio, top 3, all from the parameters), fallback to the network reference, pooled benchmarks and the ticket/attach gaps of D8
- [ ] 10.2 Stores tab: network → store table (Compras, Ticket, Itens/compra, Margem %, Cobertura cupom, Lojas parecidas with similarity %, Δ ticket vs parecidas) and, for a selected store, its profile and "Loja × parecidas × rede (referência)"
- [ ] 10.3 Verify: a store with fewer than two eligible peers shows "sem lojas parecidas suficientes" and the network reference; the network figure is labelled "referência", never "meta"
- [ ] 10.4 Benchmark order per D20: the store's own history, then similar stores, then the network as a labelled secondary reference; a view listing the stores used as similar and the main factors of similarity (demand-pattern shape, size ratio, minimum purchases), so it is never a black box

## 11. Opportunities and drill-down (on hold until 5.6)

- [ ] 11.1 `opportunities.ts`: the detectors and impact formulas of D10 and D17, the capture range and priority thresholds from the parameters, the Low-confidence cap, the caps on the number of items, category-filter matching, and the insufficient-data list
- [ ] 11.2 `copy.ts` templates for evidence, interpretation, impact, recommendation and confidence (hedged, never causal, never an order), each recommendation listing the parameter values that shaped it
- [ ] 11.3 `OpportunityCard`, `OpportunityCenter` (priority ordering, filter chips, "✦ Sugestão IA" mark) and `EvidenceSheet` with the Rede › Loja › Produto › Evidência drill-down and up to 10 real sample baskets; "por que Média?" lists factor scores and active caps
- [ ] 11.4 "O que ainda não sabemos" block, including the standing limits of D9, and wire the "Oportunidades identificadas" KPI
- [ ] 11.5 Verify: a large-impact Low-confidence opportunity is capped at Medium priority and marked to be validated first; the worked example of D17 (600 baskets, 30% versus 58%, R$ 3,00) gives R$ 504 of gap, R$ 101–302 of impact and Medium priority against a R$ 40,000 monthly margin; an opportunity with no observable gap reads "Impacto não estimável"; changing the capture range changes the printed range and the priority; the page issues only read requests across every filter and tab
- [ ] 11.6 Insight versus recommendation (D20): observations are shown as insights with evidence and confidence and no action; only a rule that links an observation to a concrete, testable action makes a recommendation; an insight can be viewed without being ranked
- [ ] 11.7 Consolidation by cause and ranking: detectors that describe the same cause on the same product, category or store become one opportunity listing its supporting detectors; within a priority the order weighs expected impact, confidence, effort and risk to margin or loss with explainable weights that are analytic parameters (provisional); a R$ 500 estimate with Low confidence must rank below a R$ 350 one with High confidence
- [ ] 11.8 The main screen shows the number of opportunities set by the business rule (default 5) and offers the rest on demand; each opportunity states its origin (baskets, product, time, similar store, loss, margin, mix); when nothing reaches its evidence minimum it says "não há evidência suficiente para recomendar uma ação" and lists what is missing, instead of filling the screen
- [ ] 11.9 Explainability and versioning: opening a recommendation shows the data behind it, the period, the benchmark, the sample size, the confidence factors and the limitations, plus the logic version and the parameter values in force; impact shown as the three scenarios with the premise stated

## 12. Documentation

- [~] 12.1 Update `frontend/apps/admin/CLAUDE.md`: route count 20 → 21, the Operação row, and a new section covering the page, the deterministic engine, the parameters and their provisional status, the coverage gate, the limits (catalog vocabulary, history, monthly loss) and the two `/sales` defects found _Written for what exists (route count 21, the Operação row and a section on the page, the pure engine, the parameters and their provisional status, the coverage gate, margin, wall-clock, data flow, synthetic guard, confidence, limits and the two `/sales` defects); the analytic parts are added when groups 6 to 11 land._
- [~] 12.2 Update `frontend/apps/admin/DESIGN.md`: the "✦ Sugestão IA" pattern (icon in primary, label in foreground, left accent on the recommendation block) and the Observado/Estimado and confidence badge semantics _The badges, the confidence levels, the quality banner and the ✦ pattern are documented; the left accent on a recommendation block is documented as to be applied when the recommendations leave hold._
- [ ] 12.3 Sync `design.md` thresholds and the confidence table with `parameters.ts` and `confidence.ts` after the calibration of group 5
- [x] 12.4 Report to the user, without editing them, the documentation drifts found: `add-sales-transaction-detail` still lists `Cupom` as not read, and `gateway-service/CLAUDE.md` documents price and central-stock routes the gateway does not implement _Reported to the operator in the hand-over message of 2026-09-19; nothing was edited._
- [x] 12.5 Document the three kinds of rule, the "Qualidade dos dados" states, the calibration page and the dependency on `add-commercial-intelligence-governance` in `frontend/apps/admin/CLAUDE.md`, and the availability and impact-scenario patterns in `DESIGN.md` _Done (2026-09-19): the section of `CLAUDE.md` was rewritten (95 parameters, kinds, availability, impact scenarios, no browser-stored rule, the register as the pending governance change) and `DESIGN.md` gained the availability, impact-estimate and three-places patterns; the phrase about a premise typed by the viewer was removed._

## 13. Verification

- [ ] 13.1 `pnpm turbo run lint typecheck build` passes, compared against the baseline of 1.2 _Measured after groups 1 to 4, for `@agiliz/admin` against the baseline of 1.2: `tsc` clean; lint 0 errors and the same 2 warnings (both in `ingestion/page.tsx`); production `next build` clean with 27 routes, `/commercial-intelligence` among them. The gate stays open until the analytic groups exist._
- [ ] 13.2 `openspec validate add-commercial-intelligence-page --strict` passes
- [ ] 13.3 A throwaway script kept outside the repo (Node 24 native type stripping plus a small `module.registerHooks` resolver for `.ts` and `@/`) runs every hand-computed fixture listed in tasks 3.7, 4.8, 6.5, 7.3, 8.4 and 11.5 against the pure engine with in-memory fixtures; nothing is written to any database _Covers 3.7 and 4.8 so far (`verify-engine.mjs`, outside the repo); the fixtures of 6.5, 7.3, 8.4 and 11.5 come with their groups._
- [ ] 13.4 Live UI check against a **local mock gateway** kept outside the repo, serving fixtures for the endpoints the page calls, with `NEXT_PUBLIC_GATEWAY_URL` pointing at it and `NEXT_PUBLIC_ALLOW_SYNTHETIC=true` for that run only; every synthetic entity is named with a visible "[SINTÉTICO]" prefix; the running stack's databases (including `sales_transaction`) are never written; the same page against the real gateway excludes and reports any marked entity _Done for what exists (a mock gateway outside the repo with scenarios: partial, open, blocked, ungrouped, empty, transaction error, no comparison, no finance permission; every entity `[SINTÉTICO]`; no database touched)._
- [ ] 13.5 Live check in the browser (Chrome DevTools or Playwright MCP): store switch issues no new transaction request; each coverage-gate state renders; badges and the ✦ mark in light and dark; drill-down and keyboard/focus in the sheet; a performance trace shows ≲ 150 ms of scripting per scope change; a mocked 403 on finance shows "Sem permissão" in loss cells while the rest works
- [ ] 13.6 Acceptance with the real imported months: the coverage figures match the calibration report of group 5, the KPIs match `/sales`, and no synthetic entity appears
- [ ] 13.7 Annotate this file after the work: `[x]` done, or `[~]` with the reason for anything skipped, and a "Known gaps carried forward" note
