# Prisma 7's prisma-client generator emits TypeScript that is compiled with the
# app. It is regenerated by `prisma generate` during install/build, so it is
# build output, not source.
generated/
dist/
