networks:
  agiliz_network:
    external: true
    name: agiliz_network

# No database: the gateway holds no state worth persisting. Sessions live in
# iam-service so revocation is authoritative in one place.
services:
  gateway-dev:
    build:
      context: ../../..
      dockerfile: backend/apps/gateway-service/Dockerfile
      target: dev
    container_name: agiliz-gateway-dev
    environment: &gateway_env
      IAM_SERVICE_URL: http://agiliz-iam-prod:3000
      STORES_SERVICE_URL: http://agiliz-stores-prod:3000
      PRODUCTS_SERVICE_URL: http://agiliz-products-prod:3000
      INGESTION_SERVICE_URL: http://agiliz-ingestion-prod:3000
      FINANCE_SERVICE_URL: http://agiliz-finance-prod:3000
      SALES_SERVICE_URL: http://agiliz-sales-prod:3000
      SUPPLY_SERVICE_URL: http://agiliz-supply-prod:3000
      INVENTORY_SERVICE_URL: http://agiliz-inventory-prod:3000
      SUPPLIERS_SERVICE_URL: http://agiliz-suppliers-prod:3000
      TREASURY_SERVICE_URL: http://agiliz-treasury-prod:3000
      ACCOUNTING_SERVICE_URL: http://agiliz-accounting-prod:3000
      BILLING_SERVICE_URL: http://agiliz-billing-prod:3000
      CAPEX_SERVICE_URL: http://agiliz-capex-prod:3000
      PORT: 3000
      # The panel's origin as the browser sees it — admin-dev's default host
      # port. Override if admin-prod (8080) or a different host is what's
      # actually running; CORS rejects any origin that doesn't match exactly.
      ADMIN_ORIGIN: ${ADMIN_ORIGIN:-http://localhost:3000}
      # The gateway writes the uploaded bytes to object storage itself, since
      # it already holds them — re-streaming to another service would double
      # the transfer for no boundary benefit.
      AWS_REGION: us-east-1
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-agiliz}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-agiliz-dev-secret}
      AWS_S3_BUCKET: ${AWS_S3_BUCKET:-agiliz-uploads}
      AWS_S3_ENDPOINT: http://agiliz-minio:9000
      AWS_S3_FORCE_PATH_STYLE: "true"
    # The ONLY backend service publishing a host port: everything else is
    # reachable solely inside agiliz_network, through here.
    ports:
      - "${GATEWAY_HOST_PORT:-3080}:3000"
    networks:
      - agiliz_network
    restart: unless-stopped
    healthcheck:
      test:
        ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  gateway-prod:
    build:
      context: ../../..
      dockerfile: backend/apps/gateway-service/Dockerfile
      target: runtime
    container_name: agiliz-gateway-prod
    environment: *gateway_env
    ports:
      - "${GATEWAY_HOST_PORT:-3080}:3000"
    networks:
      - agiliz_network
    restart: unless-stopped
    healthcheck:
      test:
        ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
