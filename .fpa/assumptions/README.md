# Assumptions

Standing assumptions used across analyses until a fact supersedes them —
e.g. a cost of capital for ROIC comparisons, a minimum liquidity threshold,
a target CCC. Each one file, stating the value, why it was chosen, and what
would supersede it. None recorded yet — see `../business-profile.md` for
the list of inputs still missing.
