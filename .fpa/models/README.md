# Models

Versioned financial models — e.g. a network-level 3-statement model built
with the `financial-analysis` plugin's `3-statement-model` command. Each
version dated, with the assumptions and evidence it was built from. None
built yet.
