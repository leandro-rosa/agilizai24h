# Mappings

How raw source data (see `../sources/`) maps onto the retail-finance model
in `../../.claude/skills/autonomous-retail-cfo/references/` — e.g. which
source column is Net Revenue, which cost-center category rolls up to which
P&L line in `store-unit-economics.md`. Nothing mapped yet.
